name: UI guard

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  ui_guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run ui-guard
        shell: pwsh
        run: ./scripts/ui-guard.ps1
